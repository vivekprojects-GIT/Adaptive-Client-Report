# Environment + secrets
.env
.env.local
*.local

# Python build / cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/

# Frontend build artifacts
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local SQLite (legacy)
*.db
*.db-shm
*.db-wal

# Chroma RAG persistent store (re-seeded on boot)
.chroma/

# Gmail OAuth + local artifacts
credentials.json
token.json
data/generated/
*.eml

# Runtime artefacts — logs leak connection strings and host names, and
# generated reports contain client figures. Neither belongs in source
# control; the CSVs above are fixtures and deliberately do stay.
data/*.log
data/preview/
data/generated/
data/*.db
data/last_rid.txt
data/cookies.txt
